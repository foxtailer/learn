services:
  app:
    build: ./app
    container_name: cryptid_app
    expose:
      - "8000"
    command: python main.py
      
  nginx:
    image: nginx:latest
    ports:
      - "8080:80"
    volumes:
      - ./nginx_config.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app

